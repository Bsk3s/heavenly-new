import React from 'react';

/**
 * LiveKitProvider serves as a placeholder for LiveKit integration.
 * Currently, it just passes through children until full LiveKit implementation is ready.
 */
const LiveKitProvider = ({ children }) => {
  return children;
};

export default LiveKitProvider; 