/**
 * Utility functions for handling styles
 */

// WebkitLineClamp-related functions have been removed as they're no longer needed.
// The standard React Native Text component with numberOfLines prop should be used instead.

// Add some utility style functions
const getResponsiveFontSize = (size) => {
  // Simple responsive font size utility
  return size;
};

const getResponsiveSpacing = (spacing) => {
  // Simple responsive spacing utility
  return spacing;
};

// Export utility functions
export default {
  getResponsiveFontSize,
  getResponsiveSpacing
}; 